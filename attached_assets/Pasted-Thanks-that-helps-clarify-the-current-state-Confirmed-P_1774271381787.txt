Thanks — that helps clarify the current state.

Confirmed:
- PayLink uses PostgreSQL, not MySQL
- The app already has working build/start scripts
- There is no significant Replit-specific server code to remove
- The only Replit references are in vite.config.ts and are dev-only / already guarded

Please adjust the implementation plan accordingly.

New priority:
Phase 1 should focus only on production deployment readiness for app.mypaylink.app, not rebuilding app architecture.

Please do the following:

1. Audit the app specifically for production reverse-proxy readiness
Focus on:
- trusted proxy settings
- X-Forwarded-* header handling
- secure cookies/sessions behind HTTPS proxy
- absolute URL/base URL handling
- production asset serving
- upload path stability
- health endpoint
- CORS review
- environment variable expectations

2. Document the exact required production environment variables
Use PostgreSQL via DATABASE_URL, not MySQL.
Include:
- NODE_ENV=production
- HOST=127.0.0.1
- PORT=8000
- DATABASE_URL=...
- APP_BASE_URL=https://app.mypaylink.app
- PUBLIC_BASE_URL=https://mypaylink.app
- SESSION_SECRET=...
- UPLOAD_DIR=...

3. Confirm the exact commands for production
Please verify and document:
- npm install
- npm run build
- npm run start
- expected runtime port
- whether WebSocket proxy support is needed

4. Prepare the app for systemd deployment
Please provide the exact working systemd ExecStart command and any working directory assumptions.

5. Add or confirm a /health endpoint
Return simple JSON like:
{ "status": "ok" }

6. Review security items needed specifically for production
At minimum:
- secure cookie behavior
- trust proxy
- auth/session hardening
- upload hardening
- no debug leakage in production
- permission enforcement review

7. Provide Nginx compatibility notes
Please document:
- required internal port
- whether Upgrade/Connection headers are needed
- upload size guidance
- timeout guidance

Please do NOT spend time on:
- removing Replit dev plugins unless they affect production
- changing database engines
- rebuilding package scripts
- redesigning the app

Deliver:
- focused Phase 1 production-readiness audit
- code changes only where necessary
- final env example
- exact start/build commands
- systemd recommendation
- Nginx compatibility notes
- remaining blockers