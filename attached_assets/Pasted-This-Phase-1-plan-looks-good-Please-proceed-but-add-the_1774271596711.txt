This Phase 1 plan looks good. Please proceed, but add the following items so production deployment is cleaner and safer:

1. Cookie/session settings
In addition to secure: isProduction, please verify:
- httpOnly: true
- sameSite is explicitly set (Lax preferred unless a specific flow requires otherwise)
- session secret is required in production
- trust proxy behavior is verified with Nginx

2. /ready endpoint behavior
Please make /ready actually validate database connectivity with a lightweight DB check and return a non-200 status if DB is unavailable.

3. Health endpoints exclusion
Please ensure /health and /ready are excluded from auth/session requirements and any frontend catch-all routing.

4. Upload directory startup behavior
If UPLOAD_DIR does not exist, please create it safely on startup with proper error handling.
Also document file permissions expected on the VPS.

5. Error handling
Please verify production errors do not expose stack traces, SQL errors, env values, or internal file paths in API responses.

6. Absolute URL review
When adding APP_BASE_URL / PUBLIC_BASE_URL support, please audit all places where absolute URLs may be generated, not just notifications.
This includes:
- email links
- redirects
- file links if applicable
- auth/session redirects
- portal links

7. Deployment docs
In DEPLOYMENT.md please include:
- exact npm install / build / start commands
- exact expected app port
- exact working directory
- example systemd file
- example Nginx server block
- SSL/certbot note
- log locations / troubleshooting steps
- rollback procedure for a bad deploy

8. Production env validation
Please fail fast on startup if required production env vars are missing, especially:
- DATABASE_URL
- SESSION_SECRET
- APP_BASE_URL if required for notifications/links

9. Static file and upload review
Please confirm static asset paths and uploaded file serving both work correctly behind reverse proxy and HTTPS.

10. Final delivery
Please provide:
- files changed
- summary of code changes
- any manual VPS steps still required
- any remaining risks/blockers before go-live