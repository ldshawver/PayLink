name: "[DISABLED] Deploy PayLink to VPS (superseded by deploy-app.yml)"

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "This workflow is disabled. Use deploy-app.yml instead."
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "disabled"
